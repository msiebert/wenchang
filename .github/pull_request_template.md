## Linear issue

<!-- e.g. https://linear.app/mixpanel/issue/AIE-XXXX -->

## Why

<!-- Why this change is needed, in a sentence or two. -->

## Acceptance criteria → tests

| Acceptance criterion (Given/When/Then) | Test(s) |
| --- | --- |
| | |

## Docs updated

- [ ] `ARCHITECTURE.md` updated (required if this PR changes the public API, module boundaries, storage/concurrency semantics, or scope enforcement)
- [ ] ADR added under `docs/adr/` (required for any spec deviation or the changes above)
- [ ] N/A — this PR doesn't touch those surfaces

## Test-changes justification

<!-- Required only if this PR deletes, skips, xfails, or otherwise weakens an
existing test. Cite the spec.md line (`Allow-test-changes:`) that permits it. -->
